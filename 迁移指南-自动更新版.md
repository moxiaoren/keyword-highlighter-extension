# 关键词高亮扩展 · 自动更新版 迁移指南

> 适用版本：**1.6.12**（自动更新版，crx 安装）
> 扩展 ID（固定）：`pkopokckifkjajej`
> 自动更新源：GitHub Pages（`moxiaoren.github.io/keyword-highlighter-extension/`）

---

## ⚠️ 为什么需要"迁移"？

旧版是**解压加载**安装的，扩展 ID 由 Chrome 按目录路径随机生成；
新版改用 **crx 安装，ID 固定**，两者 ID 不同，所以**已存的关键词/分组/设置/重要笔记不会自动跟着过去**。

一次迁移导出导入即可，之后永久全自动更新。

---

## 一、老用户迁移（旧版 → 自动更新版）

### 1. 先在旧版导出数据（重要！先做这个）
1. 打开插件 → 右上角 **⚙️ 设置 / 选项**页
2. 找到 **导出**（JSON）→ 导出并保存一个数据文件

### 2. 卸载旧版（可选但建议，避免两个插件共存）
`chrome://extensions` → 关闭旧版开关 或 移除旧版

### 3. 安装新版 crx
1. `chrome://extensions` → 右上角打开 **开发者模式**
2. 把 `keyword-highlighter-extension-1.6.12.crx` **拖拽**到扩展页面 → 确认安装
   （若拖拽无反应，改为：先解压 crx 成文件夹，再点「加载已解压的扩展程序」选择该文件夹）

### 4. 导入数据
打开新装插件的 **选项页 → 导入** → 选择第 1 步导出的文件 → 完成

### 5. 验证
`chrome://extensions` 找到该扩展 → 应显示 ID 为 `pkopokckifkjajej`
以后发新版**无需任何操作**，Chrome 自动静默更新。

---

## 二、全新安装（小白/新用户，只需一次）
按上方「3. 安装新版 crx」即可，无需导出导入。

---

## 三、开发者：如何发布新版本
1. 用 **同一个私钥** `keyword-highlighter-extension.pem` 重新打包：
   ```
   chrome --pack-extension=<插件目录> --pack-extension-key=keyword-highlighter-extension.pem
   ```
2. 得到新版 `.crx` → 重命名为 `keyword-highlighter-extension-<新版本号>.crx`
3. 放入仓库 `gh-pages` 分支的 `release/` 目录
4. 修改 `update.xml` 的 `version` 和 `codebase` 为新版本号
5. 推送到 GitHub（`gh-pages` 分支）→ 用户自动更新

> ⚠️ **私钥是命根子**：必须妥善保存，不能丢失、不能传 GitHub/公开。
> 换私钥 = 换个新扩展 = 所有用户都需重新安装一次。

---

## 附：线上资源地址
- 更新清单：`https://moxiaoren.github.io/keyword-highlighter-extension/update.xml`
- 安装包目录：`https://moxiaoren.github.io/keyword-highlighter-extension/release/`
